{
    "name": "myappceo",
    "private": true,
    "version": "0.1.0",
    "description": "MyAppCEO platform — web app, admin panel, and backend",
    "scripts": {
        "install:all": "npm --prefix backend install && npm --prefix admin install && npm --prefix myappceo install",
        "dev": "npm --prefix myappceo run dev",
        "dev:admin": "npm --prefix admin run dev",
        "dev:backend": "npm --prefix backend run start:dev",
        "build": "npm --prefix myappceo run build",
        "build:admin": "npm --prefix admin run build",
        "build:backend": "npm --prefix backend run build",
        "typecheck": "npm --prefix myappceo run typecheck && npm --prefix admin run typecheck",
        "test": "npm --prefix myappceo test"
    }
}
